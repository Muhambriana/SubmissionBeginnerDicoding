<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="@drawable/customshape"
    android:dividerHeight="3dp" >

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/gemastik_item_photo"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        tools:src="@android:color/darker_gray" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/gemastik_item_photo"
        android:layout_toRightOf="@id/gemastik_item_photo"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/gemastik_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:textColor="@android:color/black"
            android:textColorHighlight="@android:color/holo_blue_bright"
            android:textColorHint="@android:color/holo_blue_bright"
            android:textSize="16sp"
            android:textStyle="bold"
            android:typeface="monospace"
            app:fontFamily="@font/arvo_bold"
            tools:text="@string/category_name" />

        <TextView
            android:id="@+id/gemastik_item_detail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@android:color/primary_text_light"
            app:fontFamily="sans-serif-smallcaps"
            tools:text="@string/category_detail" />
    </LinearLayout>
</LinearLayout>